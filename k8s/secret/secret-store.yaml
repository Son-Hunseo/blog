# Secret은 미리 생성되어 있어야한다.
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: aws-secrets-mgr
  namespace: blog
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-northeast-2
      auth:
        secretRef:
          accessKeyIDSecretRef:
            name: awssm-secret
            key: access-key
          secretAccessKeySecretRef:
            name: awssm-secret
            key: secret-access-key