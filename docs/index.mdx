---
title: Home
sidebar_class_name: hidden-sidebar-item
slug: /
pagination_prev:
pagination_next:
hide_title: "true"
description: Cloud, DevOps 관련 기록을 남깁니다.
---

import { useState, useEffect } from 'react';
import RandomPosts from "@site/src/components/RandomPosts";

export const TypewriterText = () => {
  const [text, setText] = useState('');
  const [isTyping, setIsTyping] = useState(true);
  const fullText = "Cloud, DevOps 관련 기록을 남깁니다.";
  
  useEffect(() => {
    if (text.length < fullText.length) {
      const timer = setTimeout(() => {
        setText(fullText.slice(0, text.length + 1));
      }, 60); // 타이핑 속도
      return () => clearTimeout(timer);
    } else {
      setIsTyping(false);
    }
  }, [text]);

  return (
    <p className="heroSubtitle">
      {text}
      <span className="typewriterCursor">&nbsp;</span>
    </p>
  );
};

<header className="heroSection">
  <h1 className="heroTitle">Pipes' Blog</h1>
  <TypewriterText />
</header>

---
## 추천 글

<RandomPosts />